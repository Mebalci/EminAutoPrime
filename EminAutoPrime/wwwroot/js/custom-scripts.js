// custom-scripts.js

// Örneğin, slider için otomatik geçiş ayarları burada tanımlanabilir
$(document).ready(function () {
    $('.carousel').carousel({
        interval: 3000 // 3 saniyede bir geçiş
    });
});